TODO:
/api/articles
/api/feed

In some cases I'm sending JSON and in others I'm sending strings, I need this to all be JSON sent back to the client. 
Make the hash write out the sha256 and not the hex.